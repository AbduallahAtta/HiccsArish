<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hiccs.arish">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ExtraCoursesDetailsActivity"></activity>
        <activity
            android:name=".activities.NewsDetailsActivity"
            android:parentActivityName=".activities.NewsActivity"
            android:theme="@style/newsDetailsActivityStyle" />
        <activity android:name=".activities.StafffActivity" />
        <activity android:name=".activities.StaffDetailsActivity" />
        <activity android:name=".activities.StudentAccountActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".activities.MapActivity" />
        <activity android:name=".activities.HicssInfoActivity" />
        <activity android:name=".activities.CoursesTableActivity" />
        <activity android:name=".activities.AboutActivity" />
        <activity android:name=".activities.ExtraCoursesActivity" />
        <activity
            android:name=".activities.NewsActivity"
            android:launchMode="singleTop"
            android:parentActivityName=".activities.HomeActivity" />
        <activity android:name=".activities.StaffActivity" />
        <activity android:name=".activities.StudentActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".activities.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.GalleryActivity" />
        <activity
            android:name=".activities.GalleryDetailsActivity"
            android:label="@string/title_activity_image_details"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>